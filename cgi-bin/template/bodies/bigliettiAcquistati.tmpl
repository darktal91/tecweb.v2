<TMPL_IF AUTENTICATO>
  <TMPL_IF NACQUISTITOT>
    <p>Finora hai acquistato:</p>
    <TMPL_LOOP NAME="INFOACQUISTI">
      <ul>Del tipo <TMPL_VAR NAME="ID" ESCAPE=HTML>, al costo di <TMPL_VAR NAME="PREZZO" ESCAPE=HTML> euro ciascuno:
        <TMPL_LOOP NAME="INFO_DATATIME">
          <li><TMPL_VAR NAME="NACQUISTI" ESCAPE=HTML> il giorno <TMPL_VAR NAME="DATE" ESCAPE=HTML> alle ore <TMPL_VAR NAME="TIME" ESCAPE=HTML></li>
        </TMPL_LOOP>
      </ul>
    </TMPL_LOOP>
  <TMPL_ELSE>
    <p>Non hai ancora acquistato nessun biglietto: <a href="acquistaBiglietti.cgi">acquistane subito uno!</a></p>
  </TMPL_IF>
<TMPL_ELSE>
  <p>Attenzione : per visualizzare i biglietti acquistati devi prima effetturare il <span xml:lang="en">login</span> </p>
</TMPL_IF AUTENTICATO>